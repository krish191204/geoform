{
  "name": "geoform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "vendor/worldengine/.venv/bin/python server/worldengine_api.py",
    "dev:all": "npm run dev:api & npm run dev",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fixtures:critique": "vite-node scripts/generate-critique-fixtures.mts",
    "aoi:init": "node scripts/aoi-init.mjs",
    "aoi:fetch": "node scripts/aoi-fetch.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^3.2.4"
  }
}
