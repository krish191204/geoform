{
  "name": "geoform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "bash scripts/dev-api.sh",
    "dev:all": "npm run dev:api & npm run dev",
    "setup:api": "bash scripts/setup-worldengine.sh",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "fixtures:critique": "vite-node scripts/generate-critique-fixtures.mts",
    "aoi:init": "node scripts/aoi-init.mjs",
    "aoi:fetch": "node scripts/aoi-fetch.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
